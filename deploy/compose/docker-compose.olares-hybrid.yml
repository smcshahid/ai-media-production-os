# AIMPOS-Spark — Olares hybrid overlay (legacy).
#
# Olares shared AI defaults are now baked into docker-compose.dev.yml.
# You no longer need this file for daily dev — use `make up-dev` instead.
#
# Kept for backwards compatibility with older docs/scripts that pass this file.
# It is a no-op when layered on top of docker-compose.dev.yml.
name: aimpos-spark
