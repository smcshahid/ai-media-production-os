server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA history fallback: unknown paths return index.html so React Router can
    # resolve client-side routes (/assets, /audit, ...).
    location / {
        try_files $uri $uri/ /index.html;
    }
}
